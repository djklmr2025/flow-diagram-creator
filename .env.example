# Variables necesarias para habilitar Biblioteca/Publicar en Vercel (NO commits con valores reales)
#
# 1) Vercel Dashboard -> Storage -> Blob -> Tokens (Read/Write)
# 2) Vercel Project -> Settings -> Environment Variables
#
BLOB_READ_WRITE_TOKEN=

# Opcional: protege POST /api/publish (evita abuso). Si lo defines, el cliente debe enviar:
# - Header: x-publish-key: <PUBLISH_KEY>
# - o query: ?key=<PUBLISH_KEY>
PUBLISH_KEY=

